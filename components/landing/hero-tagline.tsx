export function HeroTagline() {
  return (
    <p className="font-oberyn-body mt-4 max-w-md text-base leading-relaxed text-neutral-900 sm:text-lg">
      End-to-end encrypted messaging. Plaintext never leaves your device — the server only ever
      sees ciphertext.
    </p>
  );
}
